# Runtime example env (used only if you run the runtime standalone)
# Usually you set env vars in the consumer repo / CI.

# Optional override for MCP prompt/contracts loading:
# MINDTRACE_BASE_PATH=/path/to/mindtrace-ai-runtime

# Optional override for test root used by CLI run:
# MINDTRACE_TEST_ROOT=frameworks/style1-native